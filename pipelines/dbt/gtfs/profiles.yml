gtfs:
  outputs:
    dev:
      type: duckdb
      path: ../../data/dlt.duckdb
      extensions:
        - httpfs
        - spatial
      settings:
        memory_limit: "4GB"
        threads: 1
  target: dev
