version: 2
models:
- name: amenity
  config:
    alias: amenity
  description: Staging model for OSM amenity features.
- name: building
  config:
    alias: building
  description: Staging model for OSM building features.
- name: emergency
  config:
    alias: emergency
  description: Staging model for OSM emergency features.
- name: healthcare
  config:
    alias: healthcare
    enabled: false
  description: Staging model for OSM healthcare features.
- name: highway
  config:
    alias: highway
  description: Staging model for OSM highway features.
- name: historic
  config:
    alias: historic
  description: Staging model for OSM historic features.
- name: leisure
  config:
    alias: leisure
  description: Staging model for OSM leisure features.
- name: office
  config:
    alias: office
  description: Staging model for OSM office features.
- name: place
  config:
    alias: place
  description: Staging model for OSM place features.
- name: public_transport
  config:
    alias: public_transport
  description: Staging model for OSM public_transport features.
- name: shop
  config:
    alias: shop
  description: Staging model for OSM shop features.
- name: tourism
  config:
    alias: tourism
  description: Staging model for OSM tourism features.
