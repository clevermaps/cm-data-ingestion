models:
- name: stops
  config:
    alias: stops
- name: stop_times
  config:
    alias: gtfs__stop_times
    enabled: false
- name: routes
  config:
    alias: gtfs__routes
    enabled: false
- name: trips
  config:
    alias: gtfs__trips
    enabled: false
