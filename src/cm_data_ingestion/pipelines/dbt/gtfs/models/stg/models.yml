models:
- name: stops
  config:
    alias: gtfs__stops
- name: stop_times
  config:
    alias: gtfs__stop_times
- name: routes
  config:
    alias: gtfs__routes
- name: trips
  config:
    alias: gtfs__trips
