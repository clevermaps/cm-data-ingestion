models:
- name: addresses__address
  config:
    alias: addresses__address
- name: base__landuse
  config:
    alias: base__landuse
    enabled: false
- name: buildings__building
  config:
    alias: buildings__building
- name: places__place
  config:
    alias: places__place
- name: divisions__division
  config:
    alias: divisions__division
- name: divisions__division_area
  config:
    alias: divisions__division_area
- name: transportation__segment
  config:
    alias: transportation__segment
